import {
  background,
  borderRadius,
  color,
  display,
  flexbox,
  fontFamily,
  fontSize,
  fontStyle,
  fontWeight,
  layout,
  letterSpacing,
  lineHeight,
  position,
  size,
  space,
  textAlign,
  textStyle,
} from 'styled-system';
import { styled } from '../../styles';

export const Box = styled.div<any>`
  ${background}
  ${borderRadius}
  ${color}
  ${display}
  ${flexbox}
  ${fontFamily}
  ${fontSize}
  ${fontStyle}
  ${fontWeight}
  ${layout}
  ${letterSpacing}
  ${lineHeight}
  ${position}
  ${size}
  ${space}
  ${textAlign}
  ${textStyle}
`;
